# Warp Terminal Configuration
# Warp 会自动读取 .zshrc，但这里是一些 Warp 特定的设置

# Warp 特性：自动补全和 AI 功能已内置
# 不需要额外配置，你的 dotfiles 配置会自动生效

# 如果你想在 Warp 中禁用某些功能，可以在这里添加
# 例如：
# export WARP_USE_SSH_WRAPPER=0

# Warp 会自动使用你的 Oh My Zsh 主题和插件
# 当前主题: robbyrussell (在 .zshrc 中定义)
