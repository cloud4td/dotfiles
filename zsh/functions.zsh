# Custom Commands and Functions
# 这些自定义命令可以安全地提交到 git

# 示例：快速跳转到常用目录
# function work() {
#     cd ~/work/talkdesk/code
# }

# 示例：快速查找进程
# function findproc() {
#     ps aux | grep -i "$1"
# }

# 示例：创建目录并进入
# function mkcd() {
#     mkdir -p "$1" && cd "$1"
# }

# 在这里添加你的自定义命令和函数
